---
name: build-mvp
description: Start building a new MVP from an idea
---

Start the FastMVP pipeline to build a complete MVP.

Use the **fastmvp** agent to orchestrate the full pipeline:
Specifier → Contract → [API Planner + Designer] → API Dev → Web Planner → Web Dev → Reviewer → Retro

Pass the user's idea: $ARGUMENTS
